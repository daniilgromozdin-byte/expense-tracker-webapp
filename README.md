# Expense Tracker — Mini App

Telegram Mini App для проекта expense-tracker.
Хостится через GitHub Pages, авторизация через Supabase Edge Function `verify-initdata`.

Исходники находятся в основном (приватном) репозитории; этот репо — только для деплоя статики.
